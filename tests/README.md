# Tests

## How to Run
Execute all repository checks from the project root:

```bash
php tests/run_all_tests.php
```

## Requirements
- PHP 7.4 CLI available in PATH
- An initialized `backend/config.php` file so existence checks pass
